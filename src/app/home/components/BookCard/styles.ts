import { styled } from "@/stitches.config";

export const BookCardContainer = styled("div", {
  display: "flex",
  width: "38rem",
  height: "12rem",
  padding: "$5 $6",
  gap: "$6",
  borderRadius: 8,

  backgroundColor: "$gray600",
});
