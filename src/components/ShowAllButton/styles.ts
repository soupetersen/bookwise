import { styled } from "@/stitches.config";

export const AllButton = styled("button", {
  display: "flex",
  width: "6.438rem",
  height: "1.875rem",

  backgroundColor: "transparent",
  border: "none",
  outline: "none",
});
